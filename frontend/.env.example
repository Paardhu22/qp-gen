NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_AWS_COGNITO_REGION=ap-south-1
NEXT_PUBLIC_AWS_COGNITO_USER_POOL_ID=ap-south-1_zHrjkaeQy
# PUBLIC app client with NO client secret. MUST equal the backend's
# AWS_COGNITO_APP_CLIENT_ID. Anything shipped here is public (it lands in the JS
# bundle), so this client must never have a secret. Leave empty until you create
# the no-secret client; the app throws a clear error rather than guessing.
NEXT_PUBLIC_AWS_COGNITO_APP_CLIENT_ID=
