Django==5.0.6
djangorestframework==3.15.2
django-cors-headers==4.4.0
python-dotenv==1.0.1
dj-database-url==2.2.0
psycopg[binary]==3.3.4
pgvector==0.2.5
openai>=2.36.0
httpx>=0.28.1
pypdf==4.2.0
PyMuPDF>=1.24.0,<1.27.0
python-docx==1.1.2
PyJWT==2.8.0
# Cognito access/ID tokens are RS256-signed. PyJWT needs `cryptography` to build
# the RSA public key from the pool JWKS (services/cognito_service.py calls
# jwt.algorithms.RSAAlgorithm.from_jwk). Without it EVERY authenticated request
# fails at validation time with ImportError / "Algorithm 'RS256' could not be found".
cryptography>=42.0.0
django-storages[boto3]==1.13.1
boto3==1.28.0
python-magic==0.4.27
